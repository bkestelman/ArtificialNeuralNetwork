#include "neuron.h"
#include "weight.h"
#include <random>
#include <vector>
#include <iostream>
using std::vector;
using std::cout;
using std::cerr;
using std::endl;

int main() {
	srand(time(NULL));
	
	vector<neuron> inputs;
	vector<neuron> outputs;
	vector<neuron> hiddens;
}
